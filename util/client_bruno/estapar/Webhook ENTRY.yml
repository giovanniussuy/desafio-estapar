info:
  name: Webhook ENTRY
  type: http
  seq: 2

http:
  method: post
  url: http://localhost:3003/webhook
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "license_plate": "ZUL0001",
        "entry_time": "2026-03-08T13:04:09.270Z",
        "event_type": "ENTRY"
      }
  auth: inherit

runtime:
  scripts:
    - type: before-request
      code: |-
        const d = new Date();
        d.setHours(d.getHours() - 3);
        bru.setVar("dateMinus3h", d.toISOString());

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
